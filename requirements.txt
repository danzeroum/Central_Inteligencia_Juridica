# Web Framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
# httpx fixado na faixa exigida simultaneamente por fastapi (>=0.23.0),
# ollama 0.1.9 (>=0.27.0,<0.28.0) e openai>=1.30 (>=0.23.0,<1.0).
# O pino anterior (0.24.1) era incompatível com ollama e quebrava
# `pip install` no CI (ResolutionImpossible).
httpx>=0.27.0,<0.28.0
# Resiliência de chamadas HTTP (retry/backoff) usada em
# src/tools/tribunal_api_adapter.py. Sem deps pesadas.
tenacity>=8.2,<10.0

# Cache & Monitoring
redis==4.6.0
prometheus-client==0.17.1

# Security
PyJWT==2.8.0
# Cifragem opcional de dados sensíveis em repouso no cache (Fernet). A chave vem
# do KeyProvider (env hoje, KMS/Vault no futuro). Veja src/utils/key_provider.py.
cryptography>=42.0,<46.0

# Observability — logging estruturado (JSON) com correlation_id.
python-json-logger>=2.0,<4.0

# CLOUD-READINESS: tracing distribuído (OPCIONAL — inerte sem
# OTEL_EXPORTER_OTLP_ENDPOINT). Descomente para habilitar exportação OTLP.
# opentelemetry-sdk>=1.27,<2.0
# opentelemetry-exporter-otlp-proto-http>=1.27,<2.0
# opentelemetry-instrumentation-fastapi>=0.48b0
# opentelemetry-instrumentation-redis>=0.48b0
# opentelemetry-instrumentation-httpx>=0.48b0

# Data Validation
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.1.0

# Environment
python-dotenv==1.0.1

# Configuration (roteamento de domínios via YAML)
PyYAML==6.0.1

# AI/ML & Vector Memory
chromadb==0.4.18
sentence-transformers==2.2.2
numpy<2.0.0,>=1.22.0

# LLM Integration
ollama==0.1.9
openai>=1.30,<2.0

# GraphQL (Onda 1 — integrações jurídicas)
strawberry-graphql[fastapi]>=0.236.0,<0.240.0

# Dependências LangChain para classificação de intenção via LLM.
#
# NOTA: estas dependências foram promovidas ao requirements.txt principal
# (o conflito httpx<0.28 foi resolvido na Frente A). Este arquivo é mantido
# para referência e instalação isolada em ambientes que não usam o compose.
#
# Em produção (docker compose) as deps já estão incluídas automaticamente.
langchain>=0.2,<0.3
langchain-openai>=0.1,<0.2

# Dependências OPCIONAIS para classificação de intenção via LLM (LangChain).
#
# O sistema funciona sem elas: src/routing/intent_classifier.py importa LangChain
# sob try/except e, na ausência, usa um fallback heurístico determinístico. Por
# isso NÃO entram no requirements.txt principal — evitam destabilizar a árvore de
# dependências fixada (langchain-openai -> openai -> httpx colide com httpx<0.28).
#
# Instale apenas se for usar um LLM real:
#   pip install -r requirements-llm.txt
langchain>=0.2,<0.3
langchain-openai>=0.1,<0.2
